# 情报监控平台技术介绍

> 面向情报业务的一体化开源情报采集、分析与研判平台

---

## 一、项目解决的痛点

### 1.1 人工监测效率低、覆盖面窄

传统情报工作中，情报员需要手动登录微博、抖音、小红书、今日头条、境外社交平台等多个网站，逐一检索、逐条阅读、手工记录。一个完整的情报周期往往需要数小时甚至数天，且极易遗漏关键信息。

**平台解决方式：** 自动化爬虫 7×24 小时覆盖 7 大社交平台 + 15 个热门话题平台，定时抓取、AI 摘要，将人工数小时的工作压缩到分钟级自动完成。

### 1.2 战略情报研判撰写周期长

战略情报报告从选题、资料搜集、信息筛选到撰写成文，通常需要资深情报分析员投入 2-3 个工作日。尤其在宗教领域战略情报攻坚战中，需要海量开源信息的交叉比对和深度研判，人力瓶颈突出。

**平台解决方式：** 用户输入情报主题后，系统自动完成「主题拆解 → 多源并发搜索 → AI 分级筛选 → 三阶段流水线撰写」全流程，生成符合公安情报文本规范的专业报告，可直接导出 Word/PDF 呈报领导。

### 1.3 舆情研判缺乏量化依据

面对海量社交帖子，哪些帖子影响力最大、哪些话题正在快速发酵，缺乏客观的量化评估体系，研判结论往往依赖主观经验。

**平台解决方式：** 基于多维互动指标（浏览、点赞、评论、转发、收藏）构建影响力评分模型，结合平台权重和时间衰减因子，实现帖子影响力的自动化量化排序。

### 1.4 多平台数据分散、无法统一管理

各平台数据格式不一、接口各异，信息孤岛严重。需要一个统一的数据中台将多源数据标准化汇聚、存储和展示。

**平台解决方式：** 统一的数据模型和 API 架构，所有平台数据归一化存储到 SQLite 数据库，前端提供统一的浏览、筛选和导出界面。

---

## 二、功能模块全景

```
┌─────────────────────────────────────────────────────────────┐
│                  星融情报监控平台                              │
├──────────┬──────────┬──────────┬──────────┬────────┬────────┤
│ 情报研判  │ 社交账号  │ 网站监测  │ 热门话题  │ 舆情搜索 │ 系统配置│
│  撰写    │   监测    │          │          │        │        │
├──────────┴──────────┴──────────┴──────────┴────────┴────────┤
│           统一爬虫引擎（降级链：OpenCLI→CDP→Scrapling→Playwright）│
│           多模型 AI 接口（MiniMax / DeepSeek / MiMo）           │
│           Web 搜索引擎（Firecrawl / Tavily）                    │
├──────────────────────────────────────────────────────────────┤
│           FastAPI 异步后端 + SQLite 持久化存储                   │
│           APScheduler 定时调度引擎                              │
│           React 18 + Ant Design 5 前端                        │
└──────────────────────────────────────────────────────────────┘
```

---

## 三、情报研判撰写模块

### 3.1 功能概述

输入情报研究主题（如「摩门教在华渗透风险分析」），系统自动完成从资料搜集到报告成文的全流程，输出结构完整、引用详实的战略情报报告。

**核心能力：**
- AI 自动将主题拆解为 8-15 条精准搜索子查询，覆盖事件、组织、法规、历史、近期动态等多个维度
- 双轨并发搜索：Web 搜索引擎（Firecrawl / Tavily）+ 社交平台爬虫（微博、抖音、小红书、今日头条、108 社区、X/Twitter）
- AI 自动筛选高相关性来源，对重要内容深度抓取全文
- 三阶段 AI 撰写流水线：事实提取 → 逐章撰写 → 统稿润色
- 三级分类管理（预设宗教领域 20 个分类，支持自定义扩展）
- 导出 Word / PDF，支持中文排版
- 报告历史查询、重新生成、删除管理

### 3.2 技术架构

#### 3.2.1 五阶段生成流水线

```
Phase 1: 查询拆解（DeepSeek，~5s）
   输入: 用户原始主题
   输出: 8-15 条 [{query, dimension}] 子查询
         ↓
Phase 2: 双轨并发搜索（~10-30s）
   ┌─ Web 引擎 ──────────────────────────────┐
   │  Firecrawl v2/search（Google/Bing 网页）  │
   │  Tavily（AI 驱动深度搜索 + 原文提取）      │
   └──────────────────────────────────────────┘
   ┌─ 社交平台爬虫 ──────────────────────────┐
   │  微博 / 抖音 / 小红书 / 头条 / 108社区 / X │
   └──────────────────────────────────────────┘
   → URL 去重合并 → 原始结果池
         ↓
Phase 3: AI 筛选 + 深度抓取（~20-40s）
   DeepSeek 评估每条结果的相关度（高/中/低）
   高相关 + 中相关 → 保留；低相关 → 丢弃
   高相关但内容过短（<500字）→ Firecrawl scrape 深度抓取全文
         ↓
Phase 4: 三阶段 AI 撰写（DeepSeek，~60-120s）
   Stage 1 — 事实提取：分批处理素材，提取事实、数据点、关联关系（JSON）
   Stage 2 — 逐章撰写：AI 自主规划章节（必含：基本信息/风险研判/对策建议），
             每章独立生成，引用来源脚注
   Stage 3 — 统稿润色：消除重复、统一术语、逻辑递进、添加日期
         ↓
Phase 5: 持久化存储
   保存 report_markdown + sources_json + progress_detail → SQLite
```

#### 3.2.2 双轨搜索引擎

| 搜索轨道 | 引擎 | 覆盖范围 | 特点 |
|---------|------|---------|------|
| Web 搜索 | Firecrawl v2 | Google / Bing 全网 | 搜索结果自带 Markdown 全文，支持单 URL 深度抓取 |
| Web 搜索 | Tavily | AI 精选网页 | 搜索深度可选 basic/advanced，自带 raw_content 原文提取 |
| 社交爬虫 | 微博 / 抖音 / 小红书 / 头条 / 108社区 / X | 中文社交平台 + 境外 X | 复用统一爬虫降级链，提取帖子文本、图片、互动数据 |

两轨并发执行，结果通过 URL 去重合并。X 平台额外支持 Firecrawl `site:x.com` 域名过滤搜索作为降级方案。

#### 3.2.3 AI 流水线技术细节

**事实提取（Stage 1）：**
- 分批处理：每批最多 15 条来源、25000 字符，防止上下文溢出
- 输出结构化 JSON：`facts`（事实清单）、`data_points`（数据点）、`relationships`（关联关系）
- 跨批次事实去重（前 60 字符匹配）

**逐章撰写（Stage 2）：**
- AI 自主规划章节结构，最低结构约束：主旨相关基本信息 → 风险隐患综合分析 → 对策建议
- 每章 800-2000 字，使用规范公安情报语言
- 每个论点引用具体来源作为脚注

**统稿润色（Stage 3）：**
- 全篇连贯性检查、重复内容消除、术语统一
- 动态插入报告生成日期

**提示词（可自定义）：**
```
你是一位精通公安情报业务的情报分析专家，具备20年公安国保/政保工作经验
和深厚的专业情报编报能力。你对宗教领域战略情报有深入研究，擅长从海量
开源信息中提取关键情报、关联碎片化线索、识别风险隐患。
```

#### 3.2.4 异步任务模型

- `POST /api/intelligence/reports/generate` → 立即返回 `{id, status: "pending"}`
- 后台 `asyncio.create_task` 异步执行五阶段流水线
- 前端每 3 秒轮询 `GET /api/intelligence/reports/{id}`，根据 `status` 和 `progress_detail` 实时更新进度
- 状态流转：`pending → searching → scraping → analyzing → writing → completed / failed`

---

## 四、社交账号监测模块

### 4.1 功能概述

添加需要持续关注的社交媒体账号（如 X/Twitter、微博、YouTube、抖音、小红书、今日头条、108 社区），系统按设定频率自动抓取最新帖子，AI 生成内容摘要，支持图文多模态分析。

**核心能力：**
- 支持 7 大社交平台账号监测
- 灵活的定时调度（cron 表达式，支持每日多次执行）
- 爬虫自动降级（OpenCLI → CDP → Scrapling → Playwright）
- AI 多模态摘要（文本 + 图片联合分析）
- 热门评论提取与排序
- 账号添加时自动预热验证

### 4.2 技术架构

#### 4.2.1 爬虫降级链

系统设计了四层爬虫引擎，按优先级依次尝试，任一层失败自动降级到下一层：

```
优先级 1: OpenCLI（复用 Chrome CDP 登录态）
   支持平台: X / 小红书 / Reddit / Bilibili
   特点: 复用已登录的 Chrome 浏览器会话，无需重复登录
       ↓ 失败
优先级 2: CDP 直连（Chrome DevTools Protocol）
   支持平台: X
   特点: 直接通过 WebSocket 控制 Chrome 实例
       ↓ 失败
优先级 3: Scrapling（隐身浏览器）
   支持平台: 今日头条
   特点: 内置反检测机制，无需 Chrome 实例
       ↓ 失败
优先级 4: Playwright（无头浏览器）
   支持平台: X / YouTube / 小红书 / 抖音 / 微博 / 今日头条 / 108社区
   特点: 兜底方案，兼容性最广
```

每一层在尝试前会检查可用性（如 Chrome 是否运行、CDP 端口是否开放），失败原因记录到 `error_log`，成功方法记录到 `crawl_method`，便于运维排查。

#### 4.2.2 多模态 AI 摘要

```
爬取帖子（含图片 URL）
     ↓
构建多模态 content 数组:
  [{type: "text", text: "帖子内容"}, {type: "image_url", image_url: {url: "图片1"}}, ...]
     ↓
调用 AI API（MiniMax / DeepSeek / MiMo）
     ↓ 失败时自动降级
回退为纯文本模式（仅发送帖子文字内容）
     ↓
输出: 内容摘要（中文，300字以内）
```

- 每帖最多 5 张图片，每批次最多 10 张
- 支持微博 CDN 防盗链图片代理（通过 `/api/tools/proxy/image` 中转，添加 Referer 头）

#### 4.2.3 定时调度

基于 APScheduler 的 `AsyncIOScheduler`，每个监测目标可配置独立的 cron 表达式：

```
示例: "0 8,12,18 * * *"  → 每天 8:00、12:00、18:00 各执行一次
示例: "30 9 * * 1-5"    → 工作日上午 9:30 执行
```

支持分号分隔的多条 cron 表达式。时区为北京时间（UTC+8），内部统一使用 UTC 存储。

---

## 五、网站监测模块

### 5.1 功能概述

针对需要持续关注的网站页面（如论坛、公告栏、政策发布页），系统按设定频率自动抓取页面内容变化，AI 生成内容摘要。

**核心能力：**
- 支持任意网页 URL 监测
- 可选 CSS 选择器精确提取页面特定区域
- 基于 Playwright 的 JavaScript 渲染支持（应对动态加载页面）
- AI 自动生成内容摘要
- 定时调度（与社交账号监测共用调度系统）

### 5.2 技术架构

```
用户配置监测目标（URL + CSS 选择器 + 调度频率）
     ↓
APScheduler 定时触发
     ↓
WebsiteCrawler（Playwright 无头浏览器）
   → 加载页面 → 执行 CSS 选择器提取 → 返回页面文本
     ↓
ContentSummarizer（AI 摘要）
   → 调用当前活跃 AI 提供商 → 生成 200 字以内中文摘要
     ↓
存储到 SQLite（原始内容截断至 10000 字符 + AI 摘要）
```

**数据模型：**
| 字段 | 说明 |
|------|------|
| url | 监测目标 URL |
| css_selector | 可选，CSS 选择器精确提取区域 |
| monitor_interval_minutes | 监测间隔（分钟） |
| cron_schedule | cron 表达式（可选，优先级高于间隔） |
| is_active | 是否启用 |

---

## 六、热门话题模块

### 6.1 功能概述

一站式聚合 15 个平台的热门话题和趋势榜单，帮助情报人员快速掌握各平台舆论热点动态。

**覆盖平台（15 个）：**

| 类别 | 平台 | 抓取模式 |
|------|------|---------|
| 中文社交 | 微博、知乎、Bilibili、豆瓣（电影/图书）、雪球 | 需浏览器登录态 |
| 技术社区 | V2EX、Linux.do、StackOverflow、GitHub | 公开接口 |
| 境外平台 | Twitter/X、HackerNews、Reddit、BBC、Google Trends | 公开接口 |

**核心能力：**
- 平台级订阅管理（按需开启/关闭特定平台）
- 异步批量抓取（立即返回，后台并发执行，前端轮询刷新）
- 排名、热度值、平台专属配色展示
- 平台级定时调度
- 批量清理和单条删除

### 6.2 技术架构

#### 6.2.1 双模式抓取引擎

```
┌─ public 模式（无需浏览器）──────────────────────────┐
│  V2EX / HackerNews / Reddit / BBC / Google Trends   │
│  StackOverflow / GitHub / Linux.do                   │
│  → AutoCLI subprocess 并发执行                        │
│  → GitHub 走独立 HTTP 爬虫（httpx + BeautifulSoup）    │
└──────────────────────────────────────────────────────┘

┌─ browser 模式（需 Chrome CDP 登录态）────────────────┐
│  微博 / 知乎 / Bilibili / Twitter / 豆瓣 / 雪球        │
│  → AutoCLI subprocess 串行执行（复用同一 Chrome 上下文） │
│  → 微博降级: Playwright → m.weibo.cn API              │
└──────────────────────────────────────────────────────┘
```

- **public 模式** 平台之间并发执行（`asyncio.gather`），无需共享浏览器状态
- **browser 模式** 平台之间串行执行，因为共享 Chrome CDP 上下文，避免并发冲突
- 单平台超时限制 90 秒

#### 6.2.2 AutoCLI 集成

AutoCLI 是一个外部命令行工具，通过 `subprocess.run()` 调用，输出标准 JSON：

```bash
autocli weibo --format json --limit 50
```

输出经过 `_normalize_topics()` 统一字段名（各平台字段名不一：`title`/`word`/`topic`、`url`/`link`、`hot_value`/`heat`/`score` 等），归一化后存入数据库。

#### 6.2.3 GitHub Trending 独立爬虫

GitHub 不走 AutoCLI，使用独立的 HTTP 爬虫：
- `httpx` 请求 `https://github.com/trending`
- `BeautifulSoup` 解析 HTML，提取仓库名、描述、语言、Star 数、Fork 数、今日新增 Star

---

## 七、舆情搜索模块

### 7.1 功能概述

基于关键词在 5 个中文社交平台实时搜索帖子，自动计算每条帖子的综合影响力评分，按影响力排序展示，帮助情报人员快速定位高价值舆情信息。

**覆盖平台（5 个）：**

| 平台 | 可用指标 | 搜索方式 |
|------|---------|---------|
| 微博 | 点赞、评论、转发 | AutoCLI（降级: Playwright） |
| 抖音 | 点赞、评论、转发 | DouyinCrawler |
| 小红书 | 点赞、评论、收藏 | OpenCLI |
| 今日头条 | 评论 | Scrapling / Playwright |
| 108 天台社区 | 评论 | Playwright |

### 7.2 技术架构

#### 7.2.1 影响力评分模型

```
impact_score = engagement_score × platform_weight × time_decay × 100
```

**① engagement_score（互动评分）**

对每项互动指标（浏览/点赞/评论/转发/收藏）计算百分位得分，加权平均：

| 指标 | 权重 | 说明 |
|------|------|------|
| 浏览量 | 0.5 | 基础曝光指标 |
| 点赞 | 1.0 | 轻度互动 |
| 评论 | 2.5 | 深度互动，权重较高 |
| 转发 | 3.5 | 传播裂变，权重最高 |
| 收藏 | 2.0 | 内容价值指标 |

百分位计算：
- **充足样本（≥1000 条）**：使用存储的分位数（p50/p75/p90/p95/p99）进行分段线性插值，精度高
- **冷启动（<1000 条）**：降级为 `log₁₀(value+1) / log₁₀(max+1)` 归一化
- **自适应分母**：部分平台仅返回部分指标（如头条仅有评论），系统自动调整分母，仅对有数据的指标加权平均

**② platform_weight（平台权重）**

```
platform_weight = log₁₀(MAU) / log₁₀(max_MAU)，钳位到 [0.3, 0.7]
```

| 平台 | MAU（月活跃用户） | 权重 |
|------|-----------------|------|
| 抖音 | 7.3 亿 | 0.70 |
| 微博 | 5.86 亿 | 0.67 |
| 今日头条 | 3.5 亿 | 0.62 |
| 小红书 | 3 亿 | 0.60 |
| 108社区 | 50 万 | 0.30 |

**③ time_decay（时间衰减）**

```
time_decay = e^(-λ × days_ago)，其中 λ = ln(2) / half_life_days
```

- 默认半衰期 4 天：4 天前的帖子影响力衰减至 50%
- 无发布时间的帖子默认扣减 7 天

#### 7.2.2 数据流转

```
用户输入关键词 + 选择平台
     ↓
POST /api/sentiment/search → 创建 SentimentTask（pending）
     ↓
asyncio.create_task 后台执行
   → asyncio.gather 并发搜索各平台
   → 每个平台走独立爬虫（含降级逻辑）
     ↓
结果写入 SentimentPost 表 → calculate_impact() 计算评分 → 回写
     ↓
前端每 3 秒轮询 → status=completed → 按影响力排序展示
```

#### 7.2.3 数据维护

- **分位数刷新**：每日凌晨 3:07 重新计算各平台各指标的分位数（p50/p75/p90/p95/p99），存储到 `PlatformStats` 表
- **数据清理**：每日凌晨 4:07 自动清理 30 天前的舆情任务和帖子数据

---

## 八、系统配置模块

### 8.1 功能概述

集中管理平台的所有外部服务配置和 AI 相关参数，支持运行时切换，无需重启服务。

### 8.2 配置项总览

#### 8.2.1 AI 模型提供商（三选一）

| 提供商 | 默认模型 | 认证方式 | 特点 |
|--------|---------|---------|------|
| MiniMax | MiniMax-M2.7 | Bearer Token | 支持多模态（图片分析） |
| DeepSeek | deepseek-chat | Bearer Token | 推理能力强，情报撰写专用 |
| MiMo | mimo-v2.5-pro | api-key Header | 小米大模型 |

- 可一键切换活跃提供商
- 每个提供商独立配置 API Key、Base URL、模型名
- API Key 支持一键验证（发送真实测试请求）
- API Key 脱敏显示（仅展示前 4 位 + 后 4 位）

#### 8.2.2 搜索服务提供商

| 提供商 | 用途 | 配置项 |
|--------|------|--------|
| Firecrawl | Google/Bing 网页搜索 + 全文抓取 | API Key |
| Tavily | AI 驱动网页搜索 + 深度内容提取 | API Key |

与 AI 模型提供商独立配置，作为情报研判模块的搜索引擎。

#### 8.2.3 AI 分析提示词（可自定义）

| 提示词 | 用途 | 默认要求 |
|--------|------|---------|
| 贴文分析提示词 | 社交账号帖子摘要 | 300 字以内中文摘要，含图片内容分析 |
| 网站分析提示词 | 网站内容摘要 | 200 字以内中文摘要 |
| 情报报告提示词 | 战略情报报告撰写 | 公安情报语言规范、事实支撑、深度研判、可操作建议 |

所有提示词支持在线编辑、保存、恢复默认值，修改立即生效。

#### 8.2.4 配置持久化机制

```
前端设置页面 → API 请求 → settings 路由
     ↓
读取 .env 文件 → 更新指定键值 → 写回 .env 文件
     ↓
setattr(settings, key, value) → 运行时立即生效
```

- 所有配置存储在 `.env` 文件中，服务重启后自动加载
- 运行时修改通过 `setattr` 同步到内存中的 `settings` 单例，无需重启

---

## 九、技术栈总览

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 后端框架 | FastAPI (Python 3.14) | 异步高性能 Web 框架 |
| 前端框架 | React 18 + Ant Design 5 | 企业级 UI 组件库 |
| 构建工具 | Vite | 极速前端构建，支持热更新 |
| 数据库 | SQLite (aiosqlite) | 轻量级，无需额外部署 |
| ORM | SQLAlchemy 2.0 (async) | 异步数据库访问 |
| 定时调度 | APScheduler | Cron 表达式触发，支持动态增删任务 |
| 爬虫引擎 | OpenCLI / CDP / Scrapling / Playwright | 四层降级链 |
| 搜索引擎 | Firecrawl v2 / Tavily | Web 搜索 + 全文抓取 |
| AI 接口 | MiniMax / DeepSeek / MiMo | OpenAI 兼容格式，支持多模态 |
| 文档导出 | python-docx / HTML-to-PDF | Word 和 PDF 导出 |

---

## 十、部署与访问

```
后端: http://localhost:8000   （FastAPI + Uvicorn，支持热重载）
前端: http://localhost:3000   （Vite Dev Server / 静态构建）
数据库: backend/data/intel_monitor.db  （SQLite，无需额外安装）
```

启动方式：
```bash
# 一键启动
start.bat

# 或分别启动
cd backend && python main.py      # 后端
cd frontend && npm run dev         # 前端（开发模式）
```

---

*文档版本：2026年6月22日*
